---
description: "06-02: Testes de Integração do Launcher e serviço de injeção"
dependencies: ["06-01"]
---

# Plan 06-02: Testes de Integração do Launcher e serviço de injeção

## Objective
Criar e executar a suíte de testes de integração C++ para verificar o comportamento do `ModManagerService` e `ShizukuLayerInjector`.

## Tasks

1. **`tests/test_launcher_integration.cpp`**
   - Criar arquivo de teste de integração.
   - Testar escaneamento de pasta temporária com mods fictícios e alternância de sufixo `.disabled`.
   - Testar formatação de pacotes do Genshin Impact (`PlayStore`, `GalaxyStore`, `ChinaServer`, `Bilibili`) no `ShizukuLayerInjector`.

2. **Compilação e Execução da Suíte de Testes**
   - Atualizar `CMakeLists.txt` para compilar o executável `test_launcher_integration`.
   - Executar os testes e validar que 100% dos testes passam com código de retorno 0.
